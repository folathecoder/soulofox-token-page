const ProjectSection = ({ data }) => {
  return (
    <section className="max-w-full md:h-screen">
      <div className="container">
          
      </div>
    </section>
  );
};

export default ProjectSection;
